<?xml version="1.0" encoding="utf-8"?>
<resources>
  <style name="Theme.JournalingPost" parent="android:Theme.Material.Light.NoActionBar" />

  <!--
    Widgetから起動し、launcher等の背後を残したままMaterial 3のBottomSheetを重ねるためのtheme。
    windowAnimationStyleを無効にしているのは、Activityのwindow遷移animationが動くと
    BottomSheet自身のslide-inと二重になり、擬似Dialogのpop-inのように見えるため。
  -->
  <style name="Theme.JournalingPost.MoodEntry" parent="Theme.JournalingPost">
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:windowBackground">@android:color/transparent</item>
    <item name="android:windowAnimationStyle">@null</item>
  </style>
</resources>