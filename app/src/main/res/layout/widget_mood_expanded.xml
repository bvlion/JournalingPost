<?xml version="1.0" encoding="utf-8"?>
<!--
  expandedレイアウトの外枠。compact側と同様に、背景専用のImageView(mood_background、
  srcなし。RemoteViewsでは素のViewが使えないため)とMood表示用のコンテナ(mood_list)を
  分け、android:themeでTheme.DeviceDefault.DayNightを
  適用してDynamic Colorを解決する。中身のMood毎の行はMoodWidgetProviderが
  widget_mood_expanded_item.xmlをaddViewで動的に追加する。
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@android:style/Theme.DeviceDefault.DayNight">

    <ImageView
        android:id="@+id/mood_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_widget_mood"
        tools:ignore="Overdraw,ContentDescription" />

    <LinearLayout
        android:id="@+id/mood_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="4dp" />

</FrameLayout>
