<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:theme="@style/Theme.JournalingPost">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.JournalingPost"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            taskAffinityを空にして、Widgetからの一時入力をアプリ本体のtaskへ入れない。
            GlanceのactionStartActivityはflagなしのIntentをPendingIntentへ包むだけで、
            widget hostからの起動では実質FLAG_ACTIVITY_NEW_TASK扱いになるため、既定の
            affinityのままだと既存MainActivityのtaskごと前面へ引き上げてしまう。
        -->
        <activity
            android:name=".widget.MoodEntryActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.JournalingPost.MoodEntry"
            android:windowSoftInputMode="adjustResize" />

        <receiver
            android:name=".widget.MoodWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_mood_info" />
        </receiver>
    </application>
</manifest>