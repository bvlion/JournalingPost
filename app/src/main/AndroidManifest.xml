<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".JournalingPostApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:theme="@style/Theme.JournalingPost">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.JournalingPost"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            GlanceのactionStartActivityはflagなしのIntentをPendingIntentへ包むだけで、
            widget hostからの起動では実質FLAG_ACTIVITY_NEW_TASK扱いになる。
            taskAffinityを空にしているのは、既定のaffinityのままだと既存MainActivityの
            taskごと前面へ引き上げてしまうため。
            singleTaskは、Widgetを再度タップしたときに古いinstanceの上へ積み増さず
            onNewIntentで新しいMoodを受け取るため。CLEAR_TASK等でActivityを作り直すと、
            記録処理中(LOADING)のrecord() coroutineをViewModelごとcancelしてしまう。
        -->
        <activity
            android:name=".widget.MoodEntryActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/Theme.JournalingPost.MoodEntry"
            android:windowSoftInputMode="adjustResize" />

        <receiver
            android:name=".widget.MoodWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_mood_info" />
        </receiver>
    </application>
</manifest>